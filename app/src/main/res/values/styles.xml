<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="button_index">
        <item name="android:textSize">13sp</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">10dp</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:background">@drawable/custom_button_index</item>
        <item name="android:layout_margin">3dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="button_dangky" parent="@style/Widget.AppCompat.Button.Colored">
        <item name="android:textColor">#fff</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">#04A008</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="button_dangnhap" parent="@style/Widget.AppCompat.Button.Colored">
        <item name="android:textColor">#fff</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">#0054A6</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="TagText" parent="AppTheme">
        <item name="android:textSize">13sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

</resources>
